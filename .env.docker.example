# Variables d'environnement pour Docker
# Copiez ce fichier vers .env et remplissez vos vraies valeurs

# Configuration Gmail
SMTP_USER=votre.email@gmail.com
SMTP_PASSWORD=votre-mot-de-passe-application
SMTP_FROM=votre.email@gmail.com
SESSION_SECRET=votre-cle-secrete-unique-pour-production

# Configuration de base de données (si différente)
DATABASE_URL=sqlite:./dev.db

# Instructions:
# 1. Copiez ce fichier: cp .env.docker.example .env
# 2. Éditez le fichier .env avec vos vraies valeurs
# 3. Le fichier .env sera automatiquement chargé par docker-compose
#
# Pour Gmail:
# - Activez l'authentification 2FA sur votre compte
# - Générez un mot de passe d'application sur https://myaccount.google.com
# - Utilisez ce mot de passe d'application (pas votre mot de passe Gmail)
#
# Pour SESSION_SECRET:
# - Générez une clé aléatoirement: openssl rand -hex 32
# - Ou utilisez: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
